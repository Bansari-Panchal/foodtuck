<%= %>
<%= link_to 'Add menu item', new_vendor_product_path(@vendor), class:"btn btn-primary"%><br>
<br>
<br>
<ul class="nav nav-tabs nav-line nav-color-primary bb-0",role="tablist" >
<li class="nav-item" >
 Regular
</li>
&nbsp&nbsp&nbsp&nbsp
<li class="nav-item">
Catering
</li>
</ul>
<br>
<br>
<table>
  <% @products.each do |p| %>
    
      <div class="container">
      <div class="row">
      <div class="col-sm-4">
      <%=image_tag(p.item_image.url) %>
      </div>
      <div class="col-sm-4">
        <h3><%= p.name %></h3><br>
        <%= p.cost_in_dollars %>
        <span class="badge">    <%= p.menu_category %></span><br>
        <%= p.description %><br>
        <span class="badge"><%= p.item_tags %></span><br>
        <span class="label label-danger"><%= link_to 'Edit ',new_vendor_product_path(p) %></span>
      </div>
      </div>
      </div>
      </br>
  <% end %>
  </table>